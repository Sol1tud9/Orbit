# Проверка корректности и выпуска

Проверено 12 сентября 2026 года. Достоверность геометрии обеспечивается прямым вызовом неизменённого `geometry.py`, а достоверность собственной логики — независимыми графовыми оракулами, тестами метрик и интеграционными проверками.

## Автоматические проверки ядра

`python -m pytest -q`: **98 passed**. Проверены:

- Все 2880 snapshots четырёх исходных сценариев и 8640 пар клиент/время: достижимость и минимальные hops сравнены с отдельной реализацией Floyd–Warshall, каждое ребро выбранного пути принадлежит эталонным контактам, внутренние узлы — активные спутники.
- Оптимальность длины среди минимальных hops и детерминированный tie-break сравнены с полным перебором простых путей на малых случайных графах.
- Метрики перерывов проверены Hypothesis против независимого оракула по булевым последовательностям; начало и конец горизонта не объединяются.
- Новый набор ID и два gateways, отключение обоих, события вне сетки, перекрывающиеся отказы, отсутствие наземного транзита, отмена и неизменяемость исходного сценария.
- Некорректные поля/ссылки, повторные ключи JSON, bool, нечисловые/бесконечные значения, структурные ограничения и сохранение расширений.
- API-сеансы, чужой run ID, экспорт, индексы, перезапуск, настоящее задание в отдельном процессе, гонка завершения и отмены.

Ruff и TypeScript `--noEmit` проходят. Production-сборка Vite проходит; зависимости frontend зафиксированы в package-lock.

## Контрольные результаты

В ячейке: **видимые / достижимые отсчёты; максимальный перерыв, с**. Всего по 720 отсчётов на клиента.

| Сценарий | C65 | C70 | C72 |
|---|---|---|---|
| Полный | 704 / 696; 480 | 719 / 711; 120 | 720 / 712; 120 |
| Первая очередь | 275 / 196; 34320 | 351 / 114; 39480 | 421 / 91; 47760 |
| Отказ 10 аппаратов | 609 / 571; 1440 | 650 / 582; 1440 | 670 / 594; 1200 |
| ISL 2000 км | 704 / 558; 5640 | 719 / 448; 10680 | 720 / 469; 240 |

Машиночитаемые значения и hashes исходных файлов: `tests/fixtures/control-results.json`. Это тестовые фикстуры, а не ветвления production-кода по имени сценария.

Для полного сценария доступность составляет 96,67 / 98,75 / 98,89%; все клиенты достигают цели 90%. При ISL 2000 км покрытие сохраняется, но сквозная доступность падает. Эти различия видны отдельно в UI.

## Исследовательские проверки модели

До реализации выполнены скалярная сверка геометрии по формулам PDF на 60 изменённых состояниях, проверки строгих порогов, отказа в 21600 с, окончания в 86400 с, переименований и перестановок. Материалы и результаты сохранены в `research`. Это дополнительные исследовательские запуски; не все они оформлены как отдельные pytest-тесты. Численное совпадение геометрии не заменяет проверку маршрутов и метрик.

## Проверка контейнера

Образ собран из Dockerfile и запущен через Compose. Через настоящий HTTP API рассчитаны четыре исходных и один новый сценарий. Сверены все контрольные показатели, исходный effective_scenario, количество записей экспорта, отключение двух шлюзов и изоляция сеансов. После перезапуска контейнера проверено чтение всех пяти готовых результатов.

Повторение:

```powershell
python tests/verify_deployment.py --url http://127.0.0.1:18000
docker compose -p orbita-verify restart app
python tests/verify_deployment.py --resume --url http://127.0.0.1:18000
```

Первый вызов сохраняет служебный cookie проверки только в игнорируемом `var/deployment-check.json`. Измерения без cookie: [deployment-measurements.json](deployment-measurements.json). В текущем локальном прогоне полные сценарии занимали примерно 4–6 секунд от отправки до получения экспорта. Это измерение одной машины, не SLA и не нагрузочный тест.

## Сквозная проверка UI

`web/tests/workspace.spec.ts` и `web/tests/design.spec.ts`: **8 passed за 41,8 с** на Chromium Headless 153. Проверены четыре исходных сценария, импорт/экспорт, восстановление истории, новый JSON, редактор и ревизии, изменение RAAN/фазы/событий, A/B и синхронная камера, N−1, рекомендации, применение кандидата и отмена эксперимента.

Маршрут C70 в сценарии отказов на t=58320 с содержит 18 рёбер: проверены все 19 узлов цепочки, прокрутка и выбор узла графа с клавиатуры. На 1440×900, 1280×720 и 390×844 проверены отсутствие горизонтального переполнения страницы и непересечение управления глобусом с нижней легендой. Снимки дополнительно просмотрены визуально: [основной экран](preview.png), [сравнение](comparison-preview.png), [рекомендации](recommendations-preview.png).

Первая проверка A/B обнаружила ошибку селектора теста: он выбирал скрытую таблицу редактора. Селектор ограничен областью сравнения. В визуальной проверке выявлено перекрытие управления глобусом с легендой; элементы переведены в последовательную раскладку и защищены проверкой координат. Финальный прогон прошёл полностью.

Ручной сценарий защиты: полный вариант → маршрут и худший перерыв → правка дальности или отказа → новая ревизия и расчёт → A/B → N−1 либо рекомендации → применение подтверждённого кандидата → экспорт отчёта.

## Проверка устойчивости

N−1 проверен против независимого полного пересчёта сценария с добавленным отказом. Для полной группировки проведены все 48 случаев на 720 отсчётах; отдельный полный пересчёт S44 совпал по доступности, покрытию, интервалам, максимальному перерыву и hops. Локальный запуск без HTTP занял около 12,16 с. [Результаты](resilience-measurements.json).

Дополнительные тесты проверяют запрет сравнения разных сеток/клиентов, diff по ID независимо от порядка массивов, критерии улучшения, отмену, родительские ревизии, владение экспериментами и сохранность отчёта после рестарта. Эти проверки не являются доказательством глобальной оптимальности рекомендаций.

В Docker 0.5.0 дополнительно выполнен `python tests/verify_stage5_deployment.py`: миграция существующей базы, A/B полного сценария и отказов (125 потерянных отсчётов C65), полный N−1, рекомендации для сценария отказов, сохранение кандидата как дочерней ревизии и проверка владения. N−1 занял 40,27 с через HTTP с проверками отмены и записью прогресса; рекомендации — 6,13 с. После обновления контейнера `--resume` подтвердил доступность обоих отчётов и ревизии. [Измерения](stage5-deployment-measurements.json).

## Проверка редизайна

После обновления оформления полный набор браузерных проверок прошёл: **8 passed за 45,4 с**. После уточнения начального выбора A/B отдельно повторён сценарий редактирования и сравнения: **1 passed**. TypeScript и production-сборка Vite проходят.

Проверено отсутствие служебных надписей на главном экране. Исправлены скрытие кнопки параметров при ширине 1280 px, недостаточный контраст успешной доступности и разрывы слов в заголовках мобильных таблиц. Просмотрены снимки главного экрана, редактора, сравнения и рекомендаций. Проверки геометрии и четыре контрольных результата сохранены; оформление не изменяет расчётное ядро.

Контейнер пересобран с новым интерфейсом; `/` и `/api/health` отвечают успешно. Оба сценария проверки с `--resume` подтвердили сохранность пяти расчётов, отчётов экспериментов и дочерней ревизии. Повторная проверка четырёх сценариев и рекомендаций прошла: **2 passed**. [Мобильный экран](mobile-preview.png).

## Этап 6: автоподбор и темы

Полный набор Python: **114 passed за 11,03 с**. Включены 16 новых проверок воспроизводимости seed, уникальности сетки, строгих границ настроек, сохранения отказов и дополнительных полей, исчерпания пространства, точного доминирования, полного пересчёта доказательств, отмены, лимитов API, владения и миграции SQLite v2 → v3.

Браузерный набор: **10 passed примерно за минуту**. После финальной корректировки подписей, тёмного диалога и мобильного графика повторены два сценария этапа 6: **2 passed за 20,8 с**. Проверены все три проекции в обеих темах, цвета и толщина связей, контур ID, отсутствие выделяемой надписи, сохранение темы после reload, мобильная ширина 390 px, запуск/отмена поиска, seed, экспорт, восстановление поиска и перенос кандидата в редактор. TypeScript, Ruff и production-сборка проходят.

В Docker 0.6.0 выполнен поиск шести новых сочетаний первой очереди на всей суточной сетке: 720 отсчётов × 3 клиента для каждого кандидата. Время через HTTP — **13,859 с**. Получены 5 улучшений без потерь и 4 Парето-варианта с учётом базы. Это измерение конкретного ограниченного поиска на одной машине. [Настройки, контрольные суммы и показатели](stage6-measurements.json).

Визуально просмотрены [тёмная тема со связями и ID](dark-preview.png), [автоподбор](autopick-preview.png) и [мобильная версия](mobile-preview.png). После обновления контейнера сохранены пять прежних расчётов, отчёты N−1/рекомендаций и ревизии. Новый поиск проверяется после рестарта командой `python tests/verify_stage6_deployment.py --resume`.

## Ограничения доказательств

Тесты подтверждают дискретную модель и реализованные операции, а не физическую пригодность реальной радиосети. CI-конфигурация подготовлена; удалённый GitHub Actions запуск не заявляется выполненным. Внешний сервер пока не выбран, поэтому HTTPS-конфигурация подготовлена, но реальная публичная ссылка ещё не проверена. Нагрузочные тесты больших сценариев и полный UX-аудит остаются критериями финального выпуска.
