# Статус реализации · 0.6.0

## Завершённый фундамент: этапы 0–2

- **0. Контракт:** изучены все материалы; зафиксированы математические правила, структурная валидация, схемы, контрольные результаты и SHA оригинала.
- **1. Независимое ядро:** произвольный допустимый сценарий проходит через CLI/API до маршрутов, метрик, интервалов, объяснений и обязательного экспорта. Контрольные и дополнительные проверки автоматизированы.
- **2. Рабочий веб-контур:** загрузка, фоновый запуск/отмена, карта и топология, выбор клиента/отсчёта, маршрут, причины, перерывы, история, повторный запуск, экспорт. Контейнер собран и локально запускается. Внешняя публикация требует согласованного сервера и домена; конфигурация HTTPS подготовлена.

Математический контракт и оригинал geometry.py сохранены при расширении продукта.

## Реализованный пакет: этапы 3–5

| Этап | Результат | Критерий готовности |
|---|---|---|
| 3. Проектирование и A/B | Формы окружения/плоскостей/фаз, редактирование состава и отказов, ветки ревизий, diff, два результата и общий курсор | Изменение каждого предусмотренного поля, сохранение/загрузка без потерь; сравнение всех клиентов; несовместимые условия явно выявлены; результаты не устаревают незаметно |
| 4. Визуальная среда | Согласованные глобус/карта/граф, масштабирование timeline, таблица контактов, улучшенный инспектор и клавиатурная навигация | Правильные оси ECEF; читаемый маршрут до 18 рёбер; поиск худшего перерыва без подсказок; 1280×720, 1440×900 и fallback без WebGL |
| 5. Устойчивость | Альтернативные пути, N−1, контрфактическая проверка причин и рекомендации с численными доказательствами | Каждое улучшение подтверждено полным пересчётом; потери/выигрыши показаны по всем клиентам; отмена пакетного эксперимента; нет необоснованных выводов |

Редактор, сравнение, визуальная сцена, диагностика и эксперименты выделены в отдельные компоненты. 98 тестов Python и 8 браузерных проверок прошли: изменение RAAN/фазы и событий → сохранение → новый расчёт → A/B; N−1 и рекомендации; отмена; маршрут из 18 рёбер; клавиатура и экраны 1440×900, 1280×720, 390 px. Готовые эксперименты и ревизии сохраняются в SQLite и артефактах.

Глобус — ортографическое представление трёхмерных ECEF-координат в SVG с масштабной высотой и скрытием обратной стороны; WebGL не требуется. Рекомендации ограничены объяснимыми вариантами устранения отказов, очереди и дальности ISL: глобальный оптимум и экономическая эффективность не заявляются. Разные сетки не интерполируются; B можно подготовить на сетке A и пересчитать. [Правила и руководство](DESIGN_AND_RESILIENCE.md).

## Завершённый этап 6

Автоподбор сочетаний очереди, дальности ISL и фаз плоскостей: до 24 новых вариантов с воспроизводимым seed, полный пересчёт, граница Парето по четырём критериям с включением базы, выигрыши/потери каждого клиента, история и экспорт поиска, открытие кандидата в редакторе. Строгие настройки и ресурсный бюджет проверяются до постановки задания в очередь. [Устройство и ограничения поиска](AUTOPICK.md).

Светлая палитра смягчена до тёплого серо-зелёного тона, добавлена тёмная тема с сохранением выбора. Связи и ID имеют контрастную обводку; подпись перетаскивания удалена. Согласованное воспроизведение A/B реализовано ранее.

## Следующий этап

7. Выпуск: нагрузка на целевой машине, внешняя ссылка, независимый чистый запуск, полный E2E редактирование → отказ → сравнение → вывод → экспорт, демонстрация до 5 минут. Обязательна проверка новым JSON жюри.

Исходное обоснование продукта, оценочных критериев и всех этапов: [research/proposal.md](../research/proposal.md).
